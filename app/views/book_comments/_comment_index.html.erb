<thead>
		<tr>
			<th>Comment</th>
			<th></th>
			<th>User name</th>
			<th colspan="3"></th>
		</tr>
</thead>
<tr>
	<% books.book_comments.each do |book_comment| %>
	    <td><%= book_comment.comment %></td>
	    <td></td>
	    <td><%= book_comment.user.name %></td>
	    <% if book_comment.user_id == current_user.id %>
	    <td>
	    <%= link_to "Destroy", book_book_comments_path(book_comment.id), method: :delete, remote:true %>
	   	<% end %>
	    </td> 
	</tr>
    <% end %>
